# Example app

Placeholder. The example app ships in Phase 3 (see the plan).

Planned: a minimal SwiftUI app with three screens' worth of behavior — a live scan list,
connect to a selection, and a characteristic value updating from notifications. It exists to
prove the API reads well at the call site, so it stays small on purpose.
