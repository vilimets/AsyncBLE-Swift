// Entry point of the library: adapter lifecycle, scanning, and connecting.
//
// Phase 1: `scan(services:allowDuplicates:) -> AsyncStream<Discovery>` and
// `connect(_:) async throws -> Connection` with a configurable timeout.
