// The `.raw` escape hatch: direct access to `CBPeripheral` / `CBCentralManager`.
//
// Contract to document on every member here: reading is safe; mutating connection state
// through these objects puts the library into undefined behavior.
